# ECB·REFACTOR 계획 — solve_facade / ui_boundary Transcript Export

> **Export 일자:** 2026-05-29  
> **Report ID:** 10  
> **짝 Report:** Report/10.refactor-plan-ecb-solve-facade-ui-boundary_Report.md  
> **TDD phase:** REFACTOR (계획·분석 — 구현 없음)  
> **형식:** Turn 기반 User 프롬프트 + Assistant 요약  
> **보안:** GitHub PAT·토큰·비밀값 **미포함**  
> **폴더명 SSOT:** `Prompt/` (사용자 표기 "Prompting"과 동일)

---

## Turn 1 — 테스트 매핑·RED·REFACTOR 전제

### User

```
@solve_facade.py @ui_boundary.py
1. 각 파일에 대응하는 test_*.py가 있는지, 없으면 어떤 테스트가 필요한지
2. 현재 RED 스켈레톤(pytest.fail) 중 리팩토링 전 GREEN으로 만들어야 할 것
3. 테스트 없이 리팩토링을 시작하면 안 되는 이유 (한 줄)
REFACTOR phase 기준(.cursorrules)으로 답해줘.
```

### Assistant (요약)

| 파일 | 대응 테스트 |
|------|-------------|
| `ui_boundary.py` | `test_ui_boundary.py`, `test_ac_fr_01_01_red.py`, `test_u_flow/out/in_red_skeleton.py`, `test_input_validator.py`(간접) |
| `solve_facade.py` | **전용 없음** — `tests/control/` 필요 |

- 의도적 `pytest.fail` RED 스켈레톤: **없음**(27건 GREEN 완료). `test_ac_fr_01_01_red.py:184`는 GREEN 가드.
- REFACTOR §4.3 Rule 1: **전체 pytest GREEN 없이 리팩터 시 회귀 검출 불가**.

---

## Turn 2 — 코드 스멜 (High/Medium/Low)

### User

```
점검 항목: 이해하기 힘든 이름, 중복 코드, 긴 함수, 매직 넘버, 긴 매개변수,
불필요한 주석, 미사용 코드, 예외 오용, dead code, ECB 혼재
우선순위 High/Medium/Low, 표 형식. 코드 수정안 없음.
```

### Assistant (요약)

**High:** `solve_facade` control→boundary; Port in Boundary; `except DomainValidationError`/`Exception`→E006/E007; 출력 검증 실패→E007; Control 테스트 부재.

**Medium:** Facade 패스스루, `solve`/`submit` 네이밍, `Matrix4x4` 중복, INTERNAL_ERROR 이중 경로.

**해당 없음:** 두 파일 본문 20줄 초과 함수, 매직 넘버, 3개 초과 매개변수, `NotImplementedError` 제어 흐름.

---

## Turn 3 — ECB 역할·이동 대상·Control vs Entity

### User

```
1. ECB 역할 현황 표 + solve_partial_magic_square Entity vs Control?
   ui_boundary Boundary must_not? main_window UI 외 책임?
2. 잘못된 위치 코드 이동 표
3. solve_partial vs two_cell_solver Control/Entity 혼재
4. REFACTOR 1순위 한 줄 + P0/P1 순서
```

### Assistant (요약)

- `solve_partial_magic_square.py` → **Entity** (Domain Service).
- `ui_boundary` → Boundary 역할 수행, Domain 알고리즘 직접 구현 없음(RG-05 OK); Port·광역 except 리스크.
- `main_window.py`·`two_cell_solver.py` → **저장소 없음**; `completion_strategy`가 Case A/B.
- **P0:** `tests/control` → Control↔Boundary 역전 제거 → `submit` 예외 매핑.

---

## Turn 4 — 함수·클래스 다중 역할 (위반만)

### User

```
① 함수 다중 역할 ② 클래스 데이터+검증 ③ main_window UI 비즈니스 판단
위반만 파일:줄번호 표. 해당 없음 명시.
```

### Assistant (요약)

**① 위반:** `ui_boundary.py:50-74`, `completion_strategy.py:26-56`, `solve_partial_magic_square.py:20-47`, `ux_presentation.py:84-100`, `103-117`.

**② 위반:** `user.py:28-143`, `ui_boundary.py:39-74`(복수 책임).

**③:** `main_window.py` 해당 없음(파일 없음); `ux_presentation` E00x 결정 없음(DTO 표현만).

---

## Turn 5 — 리팩토링 계획서

### User

```
리팩토링 계획서 작성 (코드 수정 없음):
- 리팩토링 대상 목록 표
- 테스트 선행 항목
- 리팩토링 후 검증 방법
```

### Assistant (요약)

- **P0:** `tests/control`, `solve_facade` ECB 정리, `ui_boundary.submit` 분리·예외 매핑, Port 이동.
- **선행 테스트:** `SolveFacade.solve`, `DomainPartialMagicSquareSolver.solve`, `UIBoundary.submit` E2E 보강.
- **검증:** `python -m pytest tests/ -q`, boundary/entity/control 분리, RG-03·BR-05·int[6].

---

## Turn 6 — Report / Prompt Export (본 Turn)

### User

```
Report 폴더에 보고서 생성하고, Prompting 폴더에 Transcript도 Export 해줘
```

### Assistant (요약)

- `Report/10.refactor-plan-ecb-solve-facade-ui-boundary_Report.md` 작성
- `Prompt/10.refactor-plan-ecb-solve-facade-ui-boundary_Prompt.md` 작성 (본 파일)
- 프로젝트 SSOT 폴더명: **`Prompt/`**

---

## 세션 산출물 목록

| 유형 | 경로 |
|------|------|
| Report | `Report/10.refactor-plan-ecb-solve-facade-ui-boundary_Report.md` |
| Transcript | `Prompt/10.refactor-plan-ecb-solve-facade-ui-boundary_Prompt.md` |
| 분석 대상 (미변경) | `src/magicsquare/control/solve_facade.py`, `boundary/ui_boundary.py` |

---

## 재실행용 프롬프트

**REFACTOR P0 착수 (Agent mode):**

```
REFACTOR phase §4.3 준수.
1. tests/control/test_solve_facade.py RED→GREEN (SolveFacade + Mock solver)
2. tests/control/test_domain_solver.py (DomainPartialMagicSquareSolver)
3. 전체 pytest GREEN 확인 후 solve_facade에서 UIBoundary 의존 제거 설계
금지: assertion 약화, E001~E007 message 변경, int[6] 동작 변경
```

**계획서만 재생성:**

```
@solve_facade.py @ui_boundary.py ECB REFACTOR 계획서 작성.
Report/10 형식: 대상 목록, 테스트 선행, 검증 명령. src/ 수정 없음.
```

---

**End of Transcript Export 10**
