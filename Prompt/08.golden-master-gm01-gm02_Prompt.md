# Golden Master GM-1·GM-2 — 대화 Transcript Export

> **Export 일자:** 2026-05-29  
> **Report ID:** 08  
> **짝 Report:** Report/08.golden-master-gm01-gm02_Report.md  
> **TDD phase:** 회귀 테스트 (Golden Master)  
> **형식:** Turn 기반 User 프롬프트 + Assistant 요약  
> **보안:** GitHub PAT·토큰·비밀값 **미포함**

---

## Turn 1 — GM-1 기준 파일 생성

### User

```
GM-1 기준 파일 생성 — Magic Square
- 입력 시나리오 5종 (정상/reverse/invalid blank/duplicate/no solution)
- stdout 또는 Result DTO serialize
- git add tests/golden_master_expected.txt
- approve 패턴 (없으면 생성, 있으면 비교, diff FAIL)
```

### Assistant (요약)

| 산출물 | 경로 |
|--------|------|
| Baseline | `tests/golden_master_expected.txt` |
| Harness | `tests/golden_master/harness.py` |
| 스크립트 | `scripts/generate_golden_master.py` |
| 설계 | `docs/golden-master-design.md` |

**실측 baseline (요약)**

- `normal_success` → `[1, 3, 2, 2, 2, 10]`
- `reverse_success` → `[3, 3, 6, 4, 4, 1]`
- `invalid_blank_count` → `INPUT_EMPTY_COUNT`
- `duplicate_number` → `INPUT_DUPLICATE`
- `no_valid_solution` → `SOLVE_IMPOSSIBLE`

**커밋:** `35326df` — `test(GM-1): add Golden Master baseline and approve pattern`

---

## Turn 2 — GM-2 테스트 코드

### User

```
GM-2 테스트 코드 — Magic Square
- @pytest.mark.golden_master
- GM-TC-01~05
- API serialization + stdout capture
- approve, --- expected / +++ actual diff
- int[6], row-major, 1-index, Case A/B, Error Contract
```

### Assistant (요약)

| 산출물 | 경로 |
|--------|------|
| 테스트 | `tests/golden_master/test_golden_master_magic_square.py` (16건) |
| 마커 | `pyproject.toml`, `tests/golden_master/conftest.py` |
| 삭제 | `test_gm_solver.py` (GM-2로 통합) |

**실행**

```powershell
python -m pytest -m golden_master -o addopts="" -v
→ 16 passed, 72 deselected
```

**커밋:** `fb19eb1` — `test(GM-2): golden_master marker tests and contract validation`

---

## Turn 3 — 보고서·커밋

### User

```
보고서 리포트 만들고 커밋
```

### Assistant (요약)

- `Report/08.golden-master-gm01-gm02_Report.md` 생성
- `Prompt/08.golden-master-gm01-gm02_Prompt.md` 생성 (본 파일)
- README Report/Prompt 표 갱신

---

## 재실행용 프롬프트

**Golden Master 검증:**

```
python -m pytest -m golden_master -o addopts="" -v
```

**baseline 갱신 (의도적 출력 변경 후):**

```
$env:GM_APPROVE="1"
python -m pytest -m golden_master -o addopts="" -v
git add tests/golden_master_expected.txt
```

---

**End of Transcript Export 08**
